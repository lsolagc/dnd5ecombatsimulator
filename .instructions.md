---
name: D&D Character Manager Instructions
description: Instruções para agentes de IA navegarem e desenvolverem no projeto D&D Rails
version: 1.0
scope: workspace
---

# Instruções para Agentes de IA - D&D Character Manager

## 📋 Resumo do Projeto

**D&D Character Manager** é um projeto Rails para gerenciar personagens e combates de Dungeons & Dragons 5e.

- **Stack**: Rails 8 + Phlex + Tailwind
- **Banco**: SQLite/PostgreSQL
- **Principais features**: Personalização de classes, criação de personagens, sistema de combate

## 🎯 Estrutura de Diretórios

```
/dnd
├── app/
│   ├── models/              # ActiveRecord (PlayerCharacter, PlayerClass, etc)
│   ├── controllers/         # Controllers Rails
│   ├── components/          # Phlex components (views)
│   ├── services/            # EncounterService, LevelUpService
│   ├── validators/          # Validadores customizados
│   ├── views/               # Views ERB (quando existe)
│   └── javascript/          # JS modules
├── config/                  # Configuração Rails
├── db/
│   ├── migrate/            # Migrations
│   ├── schema.rb           # Schema final
│   └── seeds.rb            # Seeds (dados iniciais)
├── lib/
│   ├── dice.rb             # Utilitário de dados
│   └── dice/               # Classes de roll (AttackRoll, RollResult)
├── test/                   # Testes
├── docs/                   # Documentação (LEIA ISTO!)
│
├── Gemfile                 # Dependências Ruby
├── package.json            # Dependências JS
├── Procfile.dev           # Processos para ./bin/dev
└── README.md              # README principal
```

## 📚 Antes de Desenvolver

1. **Leia a documentação:**
   - Start: [docs/README.md](docs/README.md)
   - Arquitetura: [docs/architecture/overview.md](docs/architecture/overview.md)
   - Modelos: [docs/models/data-model.md](docs/models/data-model.md)

2. **Setup local:**
   ```bash
   bundle install
   yarn install
   rails db:create db:migrate
   ./bin/dev
   ```

3. **Entenda os sistemas principais:**
   - Sistema de Classes: [docs/architecture/class-system.md](docs/architecture/class-system.md)
   - Sistema de Combate: [docs/architecture/combat-system.md](docs/architecture/combat-system.md)

## 🧭 Roteamento por Tipo de Tarefa

Antes de explorar muitos arquivos, classifique a tarefa.

### 1. Combate legado
- Use quando a demanda envolve iniciativa, ataques básicos, rounds ou o fluxo atual de encontro.
- Leia primeiro: [docs/architecture/combat-system.md](docs/architecture/combat-system.md), [app/services/encounter_service.rb](app/services/encounter_service.rb), [docs/models/data-model.md](docs/models/data-model.md)

### 2. Pipeline novo de efeitos
- Use quando a demanda envolve habilidades, cura, dano declarativo, payloads ou execução de efeitos.
- Leia primeiro: [docs/architecture/combat-effect-execution.md](docs/architecture/combat-effect-execution.md), [app/services/combat/](app/services/combat/), [docs/models/class-features-schema.md](docs/models/class-features-schema.md)

### 3. Modelagem e persistência
- Use quando a demanda envolve migrations, schema, relacionamentos ou campos novos.
- Leia primeiro: [docs/models/data-model.md](docs/models/data-model.md), [db/schema.rb](db/schema.rb), [docs/models/class-features-schema.md](docs/models/class-features-schema.md)

### 4. UI e fluxo web
- Use quando a demanda envolve controllers, Phlex, formulários ou navegação.
- Leia primeiro: [docs/components/overview.md](docs/components/overview.md), [app/controllers/](app/controllers/), [app/components/](app/components/)

## 🎯 Pacotes Mínimos de Contexto

Evite carregar contexto demais. Use o menor pacote que resolva a tarefa.

### Pacote: combate legado
- [.instructions.md](.instructions.md)
- [docs/architecture/combat-system.md](docs/architecture/combat-system.md)
- [app/services/encounter_service.rb](app/services/encounter_service.rb)

### Pacote: pipeline de efeitos
- [.instructions.md](.instructions.md)
- [docs/architecture/combat-effect-execution.md](docs/architecture/combat-effect-execution.md)
- [app/services/combat/](app/services/combat/)

### Pacote: modelagem
- [.instructions.md](.instructions.md)
- [docs/models/data-model.md](docs/models/data-model.md)
- [db/schema.rb](db/schema.rb)

## 🔑 Conceitos-Chave

### Modelos Principais

| Modelo | Responsabilidade | Localização |
|--------|------------------|-------------|
| `PlayerClass` | Define classe de personagem (Wizard, Fighter) | [app/models/player_class.rb](app/models/player_class.rb) |
| `PlayerCharacter` | Personagem do jogador com ability scores | [app/models/player_character.rb](app/models/player_character.rb) |
| `ClassLevelProgression` | Tabela de progressão (níveis 1-20) | [app/models/class_level_progression.rb](app/models/class_level_progression.rb) |
| `Combatant` | Entidade em combate (polimórfica) | [app/models/combatant.rb](app/models/combatant.rb) |

### Serviços

| Serviço | Responsabilidade | Localização |
|---------|------------------|-------------|
| `EncounterService` | Orquestra combates completos no estado atual; serviço provisório, acoplado ao restante do sistema, não alterar fora de refatoração planejada | [app/services/encounter_service.rb](app/services/encounter_service.rb) |
| `LevelUpService` | Aplica aumentos de nível | [app/services/level_up_service.rb](app/services/level_up_service.rb) |

### Restrições Importantes

- `EncounterService` deve ser tratado como implementação temporária. Se uma tarefa tocar nesse serviço, o padrão esperado é atualizar documentação ou trabalhar ao redor dele, não expandir sua lógica.
- Mudanças em `EncounterService` só devem acontecer quando o escopo incluir explicitamente a refatoração do sistema de combate.
- Novas mecânicas de combate devem preferir o pipeline `Combat::*` quando o escopo permitir.
- Ao alterar comportamento mecânico, valide regra e resultado, não apenas estrutura de código.

### Lib Utilities

| Módulo | Responsabilidade | Localização |
|--------|------------------|-------------|
| `Dice` | Rolar dados genéricas | [lib/dice.rb](lib/dice.rb) |
| `Dice::AttackRoll` | Rolar ataque (d20 + mods) | [lib/dice/attack_roll.rb](lib/dice/attack_roll.rb) |
| `Dice::RollResult` | Resultado de roll (com crítico/falha) | [lib/dice/roll_result.rb](lib/dice/roll_result.rb) |

## 💡 Padrões de Desenvolvimento

### Regra Global de Migrations

- Sempre criar migrations via gerador do Rails (`bin/rails generate migration ...` ou `rails g migration ...`).
- Depois de gerar, editar o arquivo em `db/migrate/` com as mudanças necessárias.
- Não criar migrations manualmente com timestamp e nome de arquivo escritos à mão.

### 1. Adicionar Feature em Model

```ruby
# app/models/player_character.rb
class PlayerCharacter < ApplicationRecord
  belongs_to :player_class
  validates :name, presence: true
  validates :ability_scores, inclusion: { in: 1..20 }
  
  def strength_modifier
    (strength - 10) / 2
  end
end
```

### 2. Lógica Complexa → Service

```ruby
# app/services/my_service.rb
class MyService
  def initialize(param)
    @param = param
  end
  
  def call
    # lógica aqui
    result
  end
end

# Uso: MyService.new(param).call
```

### 3. View em Phlex Component

```ruby
# app/components/my_component.rb
module Components
  class MyComponent < Base
    def initialize(data)
      @data = data
    end
    
    def template
      div(class: "component") { render_content }
    end
    
    private
    
    def render_content
      # HTML aqui
    end
  end
end
```

### 4. Controller - Ação Padrão

```ruby
# app/controllers/my_resources_controller.rb
class MyResourcesController < ApplicationController
  def index
    @items = MyResource.all
  end
  
  def show
    @item = MyResource.find(params[:id])
  end
  
  def create
    @item = MyResource.new(item_params)
    if @item.save
      redirect_to @item, notice: 'Created'
    else
      render :new
    end
  end
  
  private
  
  def item_params
    params.require(:my_resource).permit(:name, :description)
  end
end
```

## 🚀 Tarefas Comuns

### Adicionar Campo a Model

1. Criar migration:
   ```bash
   rails g migration AddFieldToModel field_name:type
   ```

2. Editar migration em `db/migrate/`

3. Rodar:
   ```bash
   rails db:migrate
   ```

4. Atualizar model validações

### Criar Novo Controller + Views

```bash
rails g scaffold MyResource name:string description:text
rails db:migrate
```

### Rodar Testes

```bash
rails test                    # Todos
rails test test/models/       # Models
rails test test/services/     # Services
```

### Check Linting

```bash
./bin/rubocop
./bin/brakeman
```

## ✅ Checklist de Validação

### Ao alterar combate
1. Validar HP antes e depois no resultado ou evento.
2. Validar acerto, erro crítico e falha crítica quando aplicável.
3. Validar resistência, imunidade e vulnerabilidade se a mudança tocar dano.
4. Confirmar se a mudança entrou no fluxo legado ou no pipeline novo de forma intencional.

### Ao adicionar habilidade ou efeito
1. Confirmar contrato do payload.
2. Validar resolução e execução do efeito.
3. Adicionar teste para o caso principal.
4. Atualizar a documentação se houver novo contrato.

### Ao alterar dados ou schema
1. Gerar migration com o Rails.
2. Verificar impacto em schema, fixtures e seeds.
3. Atualizar a documentação se o modelo conceitual mudar.

## 📤 Formato Esperado de Resposta para IA

Ao concluir uma tarefa relevante, a resposta deve informar:
- arquivos alterados;
- impacto no comportamento do sistema;
- o que foi validado;
- riscos, limitações ou pendências.

## 🔍 Como Explorar o Código

### Entender um Fluxo

```
1. Localize o controller: app/controllers/*/
2. Veja a ação: def create, def show, etc
3. Encontre o model: app/models/
4. Procure a view/component: app/components/ ou app/views/
5. Se há lógica complexa: app/services/
```

### Exemplo: Criando Personagem

```
GET /player_characters/new
  ↓
PlayerCharactersController#new
  ↓
render :new (busca view)
  ↓
app/views/player_characters/new.html.erb
  ↓
Renderiza form do model

POST /player_characters
  ↓
PlayerCharactersController#create
  ↓
@character = PlayerCharacter.new(character_params)
@character.save
  ↓
app/models/player_character.rb (validações, callbacks)
  ↓
Redirect v/error
```

### Buscar Onde X é Usado

```bash
# Grep para encontrar referências
grep -r "ClassName" app/

# Exemplo: encontrar uso de Combatant
grep -r "Combatant" app/
```

## ⚠️ Cuidados

1. **Migrations é history**: Nunca edite migrations antigas, crie novas
2. **Validações no model**: Sempre valide em modelo, não só controller
3. **Polimorfismo em Combatant**: Se adicionar tipo, testar com ambos tipos
4. **Hit die congelado**: ClassLevelProgression é freeze, dados imutáveis por design
5. **Testes primeiro**: Escreva testes antes de features complexas
6. **Combate legado congelado**: Não expanda `EncounterService` sem escopo explícito de refatoração
7. **Documentação é contexto operacional**: Se mudar contrato, atualizar docs faz parte da tarefa

## 🧪 Teste Localmente

```bash
# Setup
bundle && yarn && rails db:setup

# Dev server
./bin/dev

# Testes
rails test

# Console interativo
rails console
> PlayerClass.all
> PlayerCharacter.create!(...)
```

## 📖 Documentação Interna

- [docs/](docs/) - Toda documentação
- Exemplos de combate: [tmp/6_round_combat.json](tmp/6_round_combat.json)
- Schema: [db/schema.rb](db/schema.rb)

## 🆘 Problemas Comuns

| Problema | Solução |
|----------|---------|
| Migrations não rodam | `rails db:create db:migrate` |
| Componente não renderiza | Verificar caminho em `app/components/` |
| Teste falha | Executar `rails test` completo, checar fixtures |
| JS não carrega | `./bin/importmap pin` |

---

**Próximo passo?**
Leia [docs/architecture/overview.md](docs/architecture/overview.md) para entender a arquitetura completa.
